(function(){'use strict';var KEY='game-portal-nickname';var nameScreen=document.getElementById('name-screen');var gamesScreen=document.getElementById('games-screen');var input=document.getElementById('nickname');function showGames(name){sessionStorage.setItem(KEY,name);document.getElementById('player-name').textContent=name;nameScreen.classList.add('hidden');gamesScreen.classList.remove('hidden')}document.getElementById('name-form').onsubmit=function(e){e.preventDefault();var name=input.value.trim();if(name)showGames(name)};document.getElementById('rename').onclick=function(){gamesScreen.classList.add('hidden');nameScreen.classList.remove('hidden');input.focus()};document.querySelectorAll('.game-card').forEach(function(card){card.onclick=function(){location.href=card.dataset.url}});var saved=sessionStorage.getItem(KEY);if(saved){input.value=saved;showGames(saved)}var protocol=location.protocol==='https:'?'wss:':'ws:';var ws=new WebSocket(protocol+'//'+location.host+'/api/ws?game=portal');ws.onmessage=function(e){var data=JSON.parse(e.data);if(data.type!=='games')return;Object.keys(data.games).forEach(function(id){var game=data.games[id];var card=document.querySelector('.game-card.'+id);document.getElementById(id+'-status').textContent=game.status;document.getElementById(id+'-count').textContent=game.playerCount+'명';card.classList.toggle('running',game.status==='진행중');card.classList.toggle('waiting',game.status==='진행 대기중')})};setInterval(function(){if(ws.readyState===1)ws.send(JSON.stringify({type:'ping'}))},20000)})();
